#include <stdio.h>
#include <stdlib.h>

int main()
int a;
int x;
int media;
int contador;
{
for(contador=0; contador <5; contador++)
{
   printf("Ingrese un numero");
   scanf("%d", &x);
   a+=x;
}
    media =a/5;

    printf("La media de sus numeros es %d")

    return 0;
}
